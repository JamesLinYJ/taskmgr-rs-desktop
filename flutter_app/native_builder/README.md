This committed Cargokit integration builds and bundles the single `taskmgr_native`
library for Flutter desktop. The vendored build tool has one local extension:
`cargokit.yaml` may select the repository's exact Rust 1.97.1 toolchain instead
of the moving `stable` alias.
